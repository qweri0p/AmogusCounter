{
  "name": "amoguscounter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "host": "vite --host",
    "deploy": "tsc && vite build && gh-pages -d dist",
    "upload": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/three": "^0.138.0",
    "gh-pages": "^3.2.3",
    "typescript": "^4.5.4",
    "vite": "^2.9.13"
  },
  "dependencies": {
    "three": "^0.138.3"
  }
}
