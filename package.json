{
  "name": "amoguscounter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "host": "vite --host",
    "deploy": "vite build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/three": "^0.138.0",
    "gh-pages": "^3.2.3",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "three": "^0.138.3"
  }
}
