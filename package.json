{
  "name": "amoguscounter",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "host": "vite --host",
    "deploy": "tsc && vite build && gh-pages -d dist",
    "upload": "gh-pages -d dist"
  },
  "devDependencies": {
    "@types/three": "^0.144.0",
    "gh-pages": "^4.0.0",
    "typescript": "^4.8.2",
    "vite": "^3.0.9"
  },
  "dependencies": {
    "three": "^0.144.0"
  }
}
